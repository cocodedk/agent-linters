## Summary
<!-- What does this PR do and why? -->

## Changes
-

## Test plan
- [ ] Ran `lintp` on the changed files (or on a sample repo) and checked the output is still one imperative line per finding
- [ ] If `bin/lintp` or a shim (`shims/mypy`, `shims/biome`) changed: ran it directly against a real project and confirmed the wrapped tool's exit code and output are still correct
- [ ] If `install.sh` changed: re-ran it locally and confirmed the symlinks land correctly and it stays idempotent on a second run
- [ ] Shell/Python files kept LF line endings and executable bit where applicable

## Related issues
<!-- Closes #NNN -->
